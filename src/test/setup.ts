/**
 * /test/setup.ts에 setup 파일 생성
 * @testing-library/jest-dom은 Jest를 통해 테스팅을 할 때
 * DOM에 대한 Matcher와 유틸리티 함수를 제공해주는 라이브러리
 * @testing-library/jest-dom/vitest는 vitest 버전(vitest에서도 jest-dom의 매처들과 유틸리티 함수 사용)
 */

import "@testing-library/jest-dom/vitest";
